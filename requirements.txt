fastapi==0.141.1
uvicorn[standard]==0.52.0
starlette>=1.3.1,<1.4.0
mcp==1.28.1
jinja2==3.1.6
Markdown>=3.10.2,<4
python-multipart==0.0.32
itsdangerous==2.2.0
cryptography>=49.0.0,<50
onvif-zeep==0.2.12
aiohttp>=3.14.3,<4
boto3>=1.43.56,<2
paho-mqtt==2.1.0
# [QRCode] extra is required for pyhap.Accessory.xhm_uri() to work at all (it
# unconditionally calls the `base36` module, only imported if both `base36`
# and `pyqrcode` are importable) - the actual QR *rendering* still goes
# through the existing segno dependency below, not pyqrcode.
HAP-python[QRCode]==5.0.0
packaging>=26.2
# Capped below 2.3: numpy dropped the manylinux2014 (x86-64-v1 baseline) wheel
# there in favor of manylinux_2_28 only, which requires x86-64-v2 (SSE4.2/POPCNT)
# at minimum. That crashes on CPUs/VMs without it - e.g. Proxmox's default
# "kvm64"/"qemu64" CPU type instead of "host" - with "RuntimeError: NumPy was
# built with baseline optimizations: (X86_V2) but your machine doesn't support"
# (GitHub issue #31). 2.2.x still ships the compatible wheel and satisfies
# every other pinned package's numpy requirement here.
numpy>=2.2.6,<2.3
Pillow>=12.3.0,<13
onnxruntime>=1.28.0,<2
opencv-python-headless==5.0.0.93
fast-alpr[onnx]==0.4.0
# QR code rendering for the two-factor authentication setup page (pure Python,
# no transitive dependencies). The page falls back to manual secret entry if
# this is ever missing.
segno>=1.6.6,<2
