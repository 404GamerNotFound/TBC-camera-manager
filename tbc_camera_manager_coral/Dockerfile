# Local fallback for Home Assistant development. Published App images are built
# from the repository-root Dockerfile.coral so the application source remains unique.
ARG BUILD_VERSION=0.9.7
FROM ghcr.io/404gamernotfound/tbc-camera-manager-ha-coral:${BUILD_VERSION}
