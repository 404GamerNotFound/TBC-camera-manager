# Local fallback for Home Assistant development. Published App images are built
# from the repository-root Dockerfile so the application source remains unique.
ARG BUILD_VERSION=0.5.0
FROM ghcr.io/404gamernotfound/tbc-camera-manager-ha:${BUILD_VERSION}
